package frc.robot.subsystems;

public class VisionIOSim implements VisionIO {
  // In simulation no Limelight is connected; all inputs remain at default (no target).
  // Optionally publish simulated target data via SmartDashboard for manual testing.
}
